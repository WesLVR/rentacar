server:
  port: 8082

spring:
  application:
    name: clienteMS
  datasource:
    url: jdbc:mysql://localhost:3306/clientes_db
    username: root
    password: ""
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
