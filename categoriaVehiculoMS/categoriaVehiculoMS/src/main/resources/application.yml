server:
  port: 8084

spring:
  application:
    name: categoriaVehiculoMS
  datasource:
    url: jdbc:mysql://localhost:3306/categorias_vehiculo_db
    username: root
    password: ""
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
