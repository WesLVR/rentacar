server:
  port: 8085

spring:
  application:
    name: reservaMS
  datasource:
    url: jdbc:mysql://localhost:3306/reservas_db
    username: root
    password: ""
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
