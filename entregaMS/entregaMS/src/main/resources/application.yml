server:
  port: 8089

spring:
  application:
    name: entregaMS
  datasource:
    url: jdbc:mysql://localhost:3306/entrega_db
    username: root
    password: ""
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
