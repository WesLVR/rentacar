server:
  port: 8090

spring:
  application:
    name: reporteMS
  datasource:
    url: jdbc:mysql://localhost:3306/reportes_db
    username: root
    password: ""
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
